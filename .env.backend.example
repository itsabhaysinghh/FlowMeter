# Node backend (never commit actual credentials or API keys)
PORT=3001
AWS_REGION=ap-south-1
FLOW_READINGS_TABLE=flostat-flow-readings
# Recommended for efficient year-plus summary queries.
FLOW_ROLLUPS_TABLE=flostat-flow-rollups
API_KEY=replace-with-a-long-random-api-key
FRONTEND_ORIGIN=http://127.0.0.1:5173

# Simulator: API_URL must be the full backend ingress endpoint.
API_URL=http://127.0.0.1:3001/v1/flow/readings
